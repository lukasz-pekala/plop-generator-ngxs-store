import { {{firstLetterUpperCase name }}StateModel } from './{{kebabCase name }}.types';

export const default{{firstLetterUpperCase name }}State: {{firstLetterUpperCase name }}StateModel = {
  // TODO define default state
};
