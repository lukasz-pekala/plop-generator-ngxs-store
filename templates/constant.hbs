import { {{{toPascalCase name }}}StateModel } from './{{{kebabCase name }}}.types';

export const default{{{toPascalCase name }}}State: {{{toPascalCase name }}}StateModel = {
  // TODO define default state
};
