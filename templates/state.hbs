import { Injectable } from '@angular/core';
import { State } from '@ngxs/store';
import { default{{FirstLetterUpperCase name }}State } from './{{KebabCase name }}.constant';
import { {{FirstLetterUpperCase name }}StateModel } from './{{KebabCase name }}.types';


@State<{{FirstLetterUpperCase name }}StateModel>({
  name: '{{FirstLetterLowerCase name }}',
  defaults: default{{FirstLetterUpperCase name }}State,
})
@Injectable()
export class {{FirstLetterUpperCase name }}State {
  constructor() {}

}
