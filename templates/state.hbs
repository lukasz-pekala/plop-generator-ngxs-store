import { Injectable } from '@angular/core';
import { State } from '@ngxs/store';
import { default{{toPascalCase name }}State } from './{{kebabCase name }}.constant';
import { {{toPascalCase name }}StateModel } from './{{kebabCase name }}.types';


@State<{{toPascalCase name }}StateModel>({
  name: '{{toPascalCase name }}',
  defaults: default{{toPascalCase name }}State,
})
@Injectable()
export class {{toPascalCase name }}State {
  constructor() {}

}
