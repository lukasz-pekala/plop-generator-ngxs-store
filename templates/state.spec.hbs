import { Store, NgxsModule } from '@ngxs/store';
import { {{firstLetterUpperCase name }}State } from './{{kebabCase name }}.state';
import { {{firstLetterUpperCase name }}StateModel } from './{{kebabCase name }}.types';
import { fakeAsync, TestBed } from '@angular/core/testing';


describe('{{firstLetterUpperCase name }}State', () => {
  let store: Store;

  beforeEach(fakeAsync(() => {
    TestBed.configureTestingModule({
      imports: [NgxsModule.forRoot([{{firstLetterUpperCase name }}State])],
    }).compileComponents();
    store = TestBed.inject(Store);
  }));

  it('should create', () => {
    expect(store).toBeTruthy();
  });

  it('should have an initial state', () => {
    expect(store.selectSnapshot({{firstLetterUpperCase name }}State)).toEqual<{{firstLetterUpperCase name }}StateModel>({});
  });
});
